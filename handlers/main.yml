---
- name: Restart AdGuard Home Docker Compose deployment
  ansible.builtin.command:
    cmd: docker compose --ansi never --progress plain restart
    chdir: "{{ adguard_docker_path }}"
  changed_when: true
  listen: restart adguard
